# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
venv312/
pip312.log
flutter_install.log
server.pid
body.json
.env

# Everyday Buddy secrets — NEVER commit the real file (SECURITY.md)
config/security.yaml
*.pem
*.key
certs/*.pem
certs/*.key

# Runtime
logs/*.jsonl
!logs/.gitkeep
logs/*.log
*.wav
~/buddy-workspace/
buddy-workspace/

# Local model binaries (downloaded per TESTING.md / HARDWARE_VERIFICATION.md)
voice/models/
*.onnx
*.onnx.json

# OS / IDE
.DS_Store
Thumbs.db
.vscode/
.idea/
.opencode/

# Flutter build artifacts (SDK-verified locally; CI rebuilds from pubspec)
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
build/
mobile/build/
mobile/.dart_tool/
mobile/pubspec.lock
